// Find big and small elements of a number array

let arr = [10, 21, 30,1,2,3];

for (let element of arr) {
  if (element % 2 == 0) console.log(element, " is even");
  else console.log(element, " is odd");
}
